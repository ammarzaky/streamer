import test from 'node:test'; import assert from 'node:assert/strict'; import { loadConfig, toClientConfig, validateConfig } from '../../src/config.js';
test('config applies env, strips docs and freezes', async () => { const c = await loadConfig({ cwd: process.cwd(), env: { STREAMER_PORT: '9000', STREAMER_TURN_ENABLED: '0' } }); assert.equal(c.server.port, 9000); assert.equal('_comment' in c, false); assert.ok(Object.isFrozen(c.rooms)); assert.equal(toClientConfig(c).iceServers.some((x) => 'credential' in x), false); });
test('empty room grace exceeds host grace', async () => { const c = structuredClone(await loadConfig({ cwd: process.cwd(), env: {} })); c.rooms.emptyRoomGraceMs = c.rooms.hostGraceMs; assert.throws(() => validateConfig(c), /rooms\.emptyRoomGraceMs/); });
