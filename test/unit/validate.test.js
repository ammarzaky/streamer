import test from 'node:test'; import assert from 'node:assert/strict'; import { C2S, ERRORS, PROTOCOL_VERSION, STATE } from '../../public/shared/protocol.js'; import { validateMessage } from '../../src/signaling/validate.js';
const raw = (type, data) => JSON.stringify({ v: PROTOCOL_VERSION, type, data });
test('all client message types have payload validation', () => { const cases = [[C2S.CREATE_ROOM,{name:'A'}],[C2S.JOIN,{roomId:'abc',name:'B'}],[C2S.LEAVE,{}],[C2S.END,{}],[C2S.MUTE_STATE,{micMuted:true}],[C2S.CLAIM_SHARE,{force:false}],[C2S.RELEASE_SHARE,{epoch:1}],[C2S.OFFER,{to:'p',description:{type:C2S.OFFER,sdp:''}}],[C2S.ANSWER,{to:'p',description:{type:C2S.ANSWER,sdp:''}}],[C2S.ICE_CANDIDATE,{to:'p',candidate:null}],[C2S.PING,{}]]; for (const [type,data] of cases) assert.equal(validateMessage(raw(type,data), type === C2S.CREATE_ROOM || type === C2S.JOIN ? STATE.UNJOINED : STATE.JOINED).ok, true, type); });
test('wrong state and malformed json are named', () => { assert.equal(validateMessage(raw(C2S.LEAVE, {}), STATE.UNJOINED).code, ERRORS.WRONG_STATE); assert.equal(validateMessage('{', STATE.UNJOINED).code, ERRORS.INVALID_JSON); });
