const levels = Object.freeze({ debug: 10, info: 20, warn: 30, error: 40 });
export function createLogger(level = 'info') { const threshold = levels[level] ?? levels.info; const logger = {}; for (const [name, value] of Object.entries(levels)) logger[name] = (message, fields = {}) => { if (value < threshold) return; const safe = sanitize(fields); console.log(JSON.stringify({ ts: new Date().toISOString(), level: name, message, ...safe })); }; return logger; }
function sanitize(value) { if (!value || typeof value !== 'object') return value; const out = {}; for (const [key, item] of Object.entries(value)) { if (/sdp|candidate/i.test(key)) continue; if (/roomId/i.test(key) && typeof item === 'string') out[key] = `${item.slice(0, 6)}…`; else out[key] = typeof item === 'object' ? sanitize(item) : item; } return out; }
